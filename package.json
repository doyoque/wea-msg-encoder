{
  "private": true,
  "scripts": {
    "docker-build": "docker build -t wea-msg:node16.15-alpine .",
    "docker-run": "docker run -d --name wea-msg -p 3000:3000 wea-msg:node16.15-alpine",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "nuxt": "3.0.0-rc.3",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "@formkit/nuxt": "^1.0.0-beta.8"
  }
}
